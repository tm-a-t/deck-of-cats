# Test Report 0003: Telegram chat model scope final validation
Scope: Task 2c7bb3a6-80b4-4de0-a772-f7d182fe0ab9
Date: 2026-03-13

## Verdict: PASS

## What Was Tested
Final validation of the Telegram chat-agent model override for chat `175504456`, covering:
- targeted unit coverage for the changed adapter and prompt builder;
- full `bot/tests/unit` regression coverage;
- live Codex-backed `CodexChatAgentAdapter.plan()` calls with a realistic two-turn conversation in the target chat, plus a task-creation request in the same chat and the same conversation in another chat.

Browser validation was skipped as not applicable because the change is backend-only.

## Commands
- `cd bot && test -x ./.venv/bin/python`
- `cd bot && ./.venv/bin/python -m pytest tests/unit/test_chat_agent_adapter.py tests/unit/test_prompt_builder.py -q`
- `cd bot && ./.venv/bin/python -m pytest tests/unit -q`
- `cd bot && ./.venv/bin/python - <<'PY' ... PY`
  The live Python harness instantiated `CodexChatAgentAdapter` with the real `ProcessRunner` and real `codex` CLI, captured the exact argv for each call, and ran:
  - target-chat conversation before the override;
  - target-chat conversation after the override;
  - resumed target-chat conversation after the override;
  - target-chat task creation after the override;
  - same conversation in chat `175504457`.

## Expected
- Conversational replies in chat `175504456` should use `gpt-5.3-codex-spark`.
- Only plain conversation in that chat should use the override.
- Task creation and downstream code-work entry should stay on the existing default model/path.
- Other chats should stay on the default model/path.
- Relevant backend tests should pass.

## Actual
- `tests/unit/test_chat_agent_adapter.py tests/unit/test_prompt_builder.py`: `13 passed`.
- `tests/unit`: `75 passed`.
- Live before/after comparison for target chat `175504456` using the same normal conversation prompt:
  - Before override: one call, `codex -a never -s workspace-write exec --json ...`, returned `action=reply`.
  - After override: first routing call still used `codex -a never -s workspace-write exec --json ...`, then the conversational reply was generated by a second call using `codex -a never -s workspace-write -m gpt-5.3-codex-spark exec --json ...`.
  - Resumed second conversational turn kept the same split: default routing resume call plus `codex -a never -s workspace-write -m gpt-5.3-codex-spark exec resume --json ...`.
- Live task-scope check in target chat:
  - A coding-style request returned `action=create_task`.
  - That request used only the default routing call `codex -a never -s workspace-write exec resume --json ...` with no `-m`.
- Live control check in chat `175504457`:
  - The same conversation used a single default routing call with no `-m`.

## Evidence
- Runtime behavior in `bot/app/infrastructure/codex/chat_agent_adapter.py` now applies the override only after the routing decision is `reply`.
- `bot/app/di.py` wires the override only for chat `175504456` and only on `CodexChatAgentAdapter`; the main `CodexCliAdapter` used for code-writing workflows remains unchanged.
- The realistic dialogue used in the live harness was:
  - Turn 1: `Привет. Давай просто поболтаем: как лучше войти в рабочий ритм, если есть всего час?`
  - Turn 2: `А если времени только 20 минут?`
  - Task request: `Сделай новую задачу: добавь кнопку Reset на главный экран с безопасной мобильной версткой.`

## Notes
- Acceptance criteria are satisfied: target-chat conversational replies use `gpt-5.3-codex-spark`, task/code paths remain on the existing default route, other chats are unchanged, and both automated and live evidence are present.
